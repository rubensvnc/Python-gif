# Python-gif
## A python program that receives images as entry, then outputs an gif made from them.

### How to run:
- Install the python library `imageio` using the command `pip3 install imageio`
- Add all of the images that you wnat to turn into a .gif into the folder 'Img'
- Compile the code, and have fun!

